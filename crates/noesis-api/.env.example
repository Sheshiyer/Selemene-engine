# Noesis API Server Configuration
# Copy this file to .env and customize for your environment

# Server Configuration
# --------------------
# Host address to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
HOST=0.0.0.0

# Port to listen on
PORT=8080

# Authentication
# --------------
# JWT secret for token signing (REQUIRED in production, minimum 32 characters recommended)
# Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production-minimum-32-chars

# Cache Configuration
# -------------------
# Redis connection URL (optional, format: redis://[username:password@]host:port[/database])
# If not set, only in-memory L1 cache will be used
# REDIS_URL=redis://localhost:6379

# CORS Configuration
# ------------------
# Comma-separated list of allowed origins for CORS
# Default allows common local development ports
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000

# Rate Limiting
# -------------
# Maximum number of requests per user per window
RATE_LIMIT_REQUESTS=100

# Rate limit window duration in seconds
RATE_LIMIT_WINDOW_SECS=60

# Request timeout in seconds
REQUEST_TIMEOUT_SECS=30

# Logging Configuration
# ---------------------
# Log level and filter
# Format: "level" or "level,crate=level"
# Levels: trace, debug, info, warn, error
# Examples:
#   RUST_LOG=info                          (all crates at info level)
#   RUST_LOG=debug                         (all crates at debug level)
#   RUST_LOG=info,noesis_api=debug         (info for all, debug for noesis_api)
#   RUST_LOG=warn,noesis_api=debug,axum=info (custom per-crate levels)
RUST_LOG=info,noesis_api=debug

# Log output format: "pretty" for development (human-readable), "json" for production
LOG_FORMAT=pretty

# Environment mode (optional, used for validation)
# Set to "production" to enforce stricter validation (e.g., require non-default JWT_SECRET)
# RUST_ENV=production
