# Selemene planning and infrastructure recovery

Observed: 2026-09-05T10:32:24.973076+00:00. Source checkout: `9a05f5cc90777f083d7065e491484ef727aeada1`. This is a fresh recovery receipt; the full seven-wave program remains open.

## Authority and preserved work

The three canonical files `README.md`, `ROADMAP.md`, and `CAPABILITY-LEDGER.md` were missing from the checkout, not deleted from the project history. They were recovered from the untracked-file tree of the preserved planning stash without applying or dropping it. The richer ISA was recovered from the same stash only after proving it contains every one of the checkout's 170 stable criterion IDs. It contains 285 historical criteria, with maximum integer ID 278; historical assertions remain dated evidence.

`ISA.md` is the acceptance ledger. The canonical roadmap supplies Wave 0–6 scope. `.planning/` supplies executable GSD state and phase files. Current source and fresh probes can refute historical status. External products remain compatibility context; this recovery does not implement their roadmaps.

## GitHub reconciliation

Master [#893](https://github.com/Sheshiyer/Selemene-engine/issues/893) and controls #896, #901, #894, #897, #913, #908, #914 are all open. Their wave meaning is preserved. `ENGINE-ISSUE-INDEX.json` was built from a fresh GitHub query: 570 open issues, 19 engines, 30 unique slots each. No engine issue was closed or duplicated.

PR #1486 targets main and adds TS capability discovery. Initial run `33339260039` failed TS formatting; the formatter-only repair at `520e439f3fb7ad79f9d838885dfe40a9d8af8801` now passes every job in run `33962684609`. PR #1487 targets #1486's branch and adds admin/API bridge capability, Python local health state, and a partial Tarot provenance slice. They are not merged and their local changes are not demonstrated in production.

## Railway mapping

Project `robust-adventure` / `11eedde4-41e6-4f51-b86b-cf77111cf592`; production environment `702b945e-2c66-4d5a-bae1-4c67ea14c3bb`. The root checkout has no local Railway link. Read commands therefore used explicit project/environment selectors.

| Service | Active deployment | Source commit | Config |
|---|---|---|---|
| ts-engines | SUCCESS | ae3e2cef402b | /ts-engines/railway.toml |
| Selemene-engine | SUCCESS | unavailable | /railway.toml |
| biofield-cv-service | SUCCESS | 0efc3d8ae939 | /python-services/railway.toml |
| Redis | SUCCESS | unavailable | image service |
| witness-agents | SUCCESS | unavailable | image service |
| suno-bridge | SUCCESS | unavailable | /railway.toml |
| Postgres | SUCCESS | unavailable | image service |

All seven service deployments report SUCCESS and their returned instances report RUNNING. `INFRASTRUCTURE-MAP.json` preserves deployment IDs and image digests. The API's source commit is unavailable; TS is at `ae3e2ce`, and Biofield CV is at a July revision. Do not infer a shared release revision.

At baseline the effective TS builder was DOCKERFILE while its source TOML declared NIXPACKS. The TS and Python effective watch-pattern arrays are empty; the baseline source files put watch rules under `[deploy.watch]` rather than documented `build.watchPatterns`. The candidate repairs the source configuration. A separately verified deployment is still required. Python source declares `/health`, but the active effective deployment manifest omits a healthcheck path. Verify current control-plane configuration before changing it.

## Cloudflare mapping

Profile `9d9d` successfully queried account `9d9d23b27f32e70ae3afb6a1aa2c0f10`. The account owns active zone `tryambakam.space` (`3c1066df55d4e99464c8bcf1f850894b`). Live Worker settings and deployments were read for `selemene-gw`, `selemene-llm-proxy`, and `selemene-admin-api-proxy`. Gateway and LLM KV namespace bindings match source. The admin route is `144.tryambakam.space/api/*`.

`selemene-pattern-memory` returns code 10007 (Worker absent); its source uses placeholder KV/D1 identifiers and declares additional resources. Keep it explicitly undeployed. DNS record listing returns authentication error 10000. That is a permission gap, not evidence of missing DNS or a reason to alter account ownership.

## Runtime probes

A standard curl to `https://selemene.tryambakam.space/health/live` returns HTTP 200, version 3.3.1, 19 engines and six workflows. Python urllib received 403/1010 on the same domain; this client-specific result does not establish API downtime. TS `/health` returns HTTP 200 with six engines; TS `/engines/capabilities` returns HTTP 404. The new endpoint is therefore not demonstrated on the active TS release.

## Phase ownership and critical gates

1. GSD 01 restores authority, current inventory and CodeGraph; it does not close every original Wave 0 exit condition.
2. GSD 02 closes remaining registry/build/CI/dependency gates before broad engine work. Source infra config repairs belong here; runtime deployment belongs in 07.
3. GSD 03 finishes real native/conditional capability coverage and contract parity. Six bridge engine rows do not cover twelve native engines plus database-conditional `biofield-capture`.
4. GSD 04–07 preserve the engine, state/auth, distribution and operational work from Waves 3–6.

Critical gates remain: a concrete merge/deployment that can trigger production, new DNS/security access, production schema/data mutation, paid generation, and irreversible operations. Routine local repairs and recommended discussion choices continue automatically. No production configuration, database, DNS, resource creation or provider generation was performed by this recovery inventory.

## Primary references

- [Railway configuration as code](https://docs.railway.com/reference/config-as-code): use build.watchPatterns and compare source with effective deployment configuration.
- [Wrangler configuration](https://developers.cloudflare.com/workers/wrangler/configuration/): explicit account and resource bindings.
- [Cloudflare API calls](https://developers.cloudflare.com/fundamentals/api/how-to/make-api-calls/): authorization is scoped; successful Worker access does not imply DNS permission.

## Phase 2 source repair and enforcement review

The recovery branch now contains infrastructure repair ee2b39f (same patch as isolated infra commit cf3881f): documented repo-root build.watchPatterns, explicit DOCKERFILE, aligned TS compatibility config, Cloudflare account_id declarations and removal of unsupported deploy.resources. The old intended1024MB/1vCPU is preserved as unverified documentation. Captured effective service manifests have limitOverride:null for API, TS and CV; no active override was inferred or applied. No live deployment/configuration changed.

PR#1486 formatter-only source520e439 passes local lint/typecheck and92tests. Remote run33962684609 completed successfully in all jobs. The recovery branch additionally contains the existing #1487 capability slice; its complete TS suite passes 93 tests.

Ruleset noesis-admin15597830 is ACTIVE on main and the default branch, with deletion and non_fast_forward rules, and no bypass actors. It has no required CI or review rule. The proposed additive CI requirement below is a critical repository-security decision; it is not applied.

The scoped API reports DNS read code10000. An authenticated in-app Cloudflare dashboard visit also displayed a page/permission error for the account/domain DNS view. That UI result does not establish whether the route or account permission caused the error; no DNS record contents were read through either surface.

Worker receipt discipline: several Claude Code/OmniRoute attempts returned final acknowledgments or admission503 without completing edits. Those were rejected. Bounded source transformation packets produced concrete config/code candidates; every accepted patch is semantically compared with its allowed change set before integration. A proposed admin fetch loop was rejected before application.

## Final local verification and browser update

The repaired candidate passes the canonical gate, 67 script checks (including 14 tests executing the actual merge-lane workflow with mocked APIs), 104 witness tests and witness build, admin lint/typecheck/build and three real admin tests. Node production/development audit counts are all zero. The newly locked Python environment passes 61 tests and a 54-package audit with zero vulnerabilities. Rust reports zero known vulnerabilities; its optional HTTP/3 lockfile yank is explicitly retained and explained in DEPENDENCY-AUDIT-2026-09-05.md.

Independent review corrected admin query-state and Clear behavior, complete release CI, fail-closed admin smoke, exact-head agent merge checks, and Python interpreter/image test coverage. The final bounded re-review passed with no findings; its limits remain recorded separately. Candidate Linux image builds and fresh remote CI are required before promotion.

The user's authenticated IAB session subsequently verified targeted DNS records in account9d9d and the deployed admin registry/sidecar views. AUTHENTICATED-ADMIN-2026-09-05.md supersedes the earlier DNS visibility gap, while preserving the API-scope failure. Runtime engine descriptions overclaimed a fixed count and Python UI health; candidate wording is corrected. The deployed admin still does not prove the new source capability endpoints or rebuilt images.

One draft recovery PR consolidates the existing capability commits and scoped repairs for review. Existing PR branches are preserved. Main promotion remains a critical action because repository push hooks and provider integrations may deploy to production. Ruleset changes remain separately reviewable in MAIN-CI-RULE-PROPOSAL.json. No original wave exit is closed from this recovery slice alone.
