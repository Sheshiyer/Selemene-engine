---
phase: 02-reproducible-gates-dependency-repair
plan: "07"
type: execute
wave: 5
depends_on: ["06"]
gap_closure: true
files_modified: ["contracts/release/v1", "scripts/validate_release_receipt.py", "tests/scripts/test_validate_release_receipt.py", "tests/scripts/test_gate_wiring.py", "package.json", ".github/workflows/deploy.yaml", ".github/workflows/release.yml"]
autonomous: true
requirements: ["GATE-05"]
user_setup: []
must_haves:
  truths:
    - "Release eligibility fails closed on missing or mismatched source, image/build, schema, service-role, test and rollback identity."
    - "Built and deployed artifact identity remain separate; source redeployment cannot be called immutable-image promotion without evidence."
    - "All verification uses fixtures, mocks or disposable services; live publication and deployment require their own concrete critical decision."
  artifacts:
    - path: "contracts/release/v1/receipt.schema.json"
      provides: "Versioned release and asset-evidence requirements"
    - path: "scripts/validate_release_receipt.py"
      provides: "Fail-closed offline eligibility validation"
  key_links:
    - from: ".github/workflows/deploy.yaml"
      to: "scripts/validate_release_receipt.py"
      via: "validation before any promotion mutation"
---
<objective>Close release/asset receipt authority and gate wiring before further engine expansion.</objective>
<context>
@ISA.md
@.planning/phases/02-reproducible-gates-dependency-repair/02-CONTEXT.md
@.planning/phases/02-reproducible-gates-dependency-repair/02-VERIFICATION.md
@docs/plans/selemene-engine/CRITICAL-GATE-2026-09-05.md
@docs/plans/selemene-engine/INFRASTRUCTURE-MAP.json
</context>
<tasks>
<task type="auto">
<name>Define and falsify release eligibility</name>
<files>contracts/release/v1, scripts/validate_release_receipt.py, tests/scripts/test_validate_release_receipt.py</files>
<read_first>contracts/v1/registries/engines.json, contracts/v1/manifest.json, .github/workflows/deploy.yaml, .github/workflows/release.yml, scripts/apply-migrations.sh, docs/plans/selemene-engine/CAPABILITY-LEDGER.md, docs/plans/selemene-engine/INFRASTRUCTURE-MAP.json</read_first>
<action>Map each required dimension to the current infrastructure map or critical-gate obligation, retaining unavailable values and justified not-applicable values distinctly. Link receipt registry and contract/schema IDs to plan06 authority, and reject mismatched identities. Include a negative fixture reflecting the currently unknown API source/schema. Define source revision, build/image digest, schema identity, service role, required check identity, optional dependency state, asset source/integrity/retention and rollback evidence. Distinguish unavailable evidence from not-applicable fields; justify exceptions rather than accepting empty values. Build a synthetic fixture set with positive and missing/mismatched cases, clearly marked as test data. Verify source redeploy and built-image identity independently. A validator must reject current incomplete production evidence, not fill its unknown fields with guessed values.</action>
<verify><automated>python3 -m pytest tests/scripts/test_validate_release_receipt.py -q; explicit wrong-source, wrong-image, missing-schema, missing-rollback and unknown-asset cases fail eligibility.</automated></verify>
<done>The versioned offline validator proves the intended fail-closed policy and exposes current missing evidence.</done>
</task>
<task type="auto">
<name>Place receipt validation before release mutations</name>
<files>package.json, tests/scripts/test_gate_wiring.py, .github/workflows/deploy.yaml, .github/workflows/release.yml</files>
<read_first>package.json, tests/scripts/test_gate_wiring.py, .github/workflows/deploy.yaml, .github/workflows/release.yml, scripts/sync-plans-to-github-issues.sh</read_first>
<action>Wire fixture validation into the repository gate and candidate eligibility validation before any provider/publication mutation in both release paths. Statically verify the workflow graph requires receipt validation before every publication/deployment mutation, retains immutable Action pins and cannot bypass it through workflow_dispatch. Use mocked workflow execution and temporary local artifacts to prove missing input prevents all mutation calls. Do not trigger CD, tags, plan-sync apply, cloud configuration or resource changes while testing. Run plan-sync in dry-run mode to make an eventual main merge's issue effects reviewable. Record any provider-side artifact promotion changes as a separate critical decision with exact targets and rollback effects.</action>
<verify><automated>pnpm run gate; workflow/action-pin validation passes; mocked missing/mismatched receipt runs make zero mutation calls and correct fixtures reach only mocked promotion.</automated></verify>
<done>Both release paths require source-bound evidence before mutation, with executable negative tests and no live promotion performed.</done>
</task>
</tasks>
<verification>Require recorded independent automated code-review outcomes (no human approval), current-source CI and a GSD verification pass before completing Phase 2. Actual production source/schema and rollback acceptance remain Phase 7 obligations.</verification>
<success_criteria>GATE-05 closes through explicit authority and executable gates; no test receipt is described as a production deployment or rollback.</success_criteria>
<output>Run the GSD verification workflow on the exact source commit after both gap plans, record independent review and current-source CI, and leave any unresolved requirement open. Only a new production/security action requires critical HITL; local review does not add a human gate. Write 02-07-SUMMARY.md; update ISA and phase verification; present any new concrete production decision only after its candidate is reviewable.</output>
