---
phase: 02-reproducible-gates-dependency-repair
plan: "06"
type: execute
wave: 4
depends_on: ["05"]
gap_closure: true
files_modified: ["contracts/v1/registries", "contracts/v1/manifest.json", "scripts/validate_contracts.py", "tests/scripts/test_validate_contracts.py", "crates/noesis-core/tests/contract_v1_authority.rs", "crates/noesis-orchestrator/src/lib.rs", "ts-engines/src/server/__tests__"]
autonomous: true
requirements: ["GATE-05"]
user_setup: []
must_haves:
  truths:
    - "Plan 05 must record a current-source green canonical/remote gate and the critical protection decision outcome before this plan starts; capture that source commit in the summary."
    - "One machine-readable authority distinguishes 19 runtime IDs, 17 public mirror groups, 12 native registrations, one database-conditional registration and six TS registrations."
    - "Missing IDs, duplicates and incorrect runtime classes fail executable registry checks; declared rows do not imply operational completion."
  artifacts:
    - path: "contracts/v1/registries/engines.json"
      provides: "Versioned registry, public grouping, ownership and evidence-axis references"
  key_links:
    - from: "scripts/validate_contracts.py"
      to: "contracts/v1/registries/engines.json"
      via: "canonical repository gate"
---
<objective>Close the remaining Wave 0 registry authority gap without expanding engine semantics.</objective>
<context>
@ISA.md
@.planning/phases/02-reproducible-gates-dependency-repair/02-CONTEXT.md
@.planning/phases/02-reproducible-gates-dependency-repair/02-VERIFICATION.md
@docs/plans/selemene-engine/CAPABILITY-LEDGER.md
@contracts/v1/manifest.json
</context>
<tasks>
<task type="auto">
<name>Bind registry rows to current runtime evidence</name>
<files>contracts/v1/registries/engines.json, contracts/v1/manifest.json</files>
<read_first>contracts/v1/manifest.json, contracts/v1/schemas/engine-capability.schema.json, crates/noesis-orchestrator/src/lib.rs, crates/noesis-api/src/lib.rs, ts-engines/src/server/registry.ts, docs/plans/selemene-engine/CAPABILITY-LEDGER.md</read_first>
<action>CodeGraph is local navigation, not a CI dependency; if unavailable, record direct inspection of the read_first sources. Use CodeGraph on this worktree to trace register_native_runtime_engines, bridge registrations and conditional Biofield capture registration. Derive stable IDs and public grouping from source and canonical evidence, resolving contradictions before writing rows. Add a versioned registry with explicit native, conditional and TS classes, owner, public grouping and six-axis evidence references or unknown status. Register the new authority through the contract manifest so the canonical validator discovers it. Reuse existing issue IDs. Do not mark a route operational because it exists.</action>
<verify><automated>JSON parses; 19 unique runtime IDs, 17 distinct public mirror groups, class counts 12/1/6; every row has owner and all six evidence-axis fields with references or explicit unknown/not-applicable status; the contract manifest loads the registry.</automated></verify>
<done>Registry classifications are traceable to executable source and retain unsupported completion claims as unknown.</done>
</task>
<task type="auto">
<name>Make registry drift fail the existing gate</name>
<files>scripts/validate_contracts.py, tests/scripts/test_validate_contracts.py, crates/noesis-core/tests/contract_v1_authority.rs, crates/noesis-orchestrator/src/lib.rs, ts-engines/src/server/__tests__</files>
<read_first>scripts/validate_contracts.py, tests/scripts/test_validate_contracts.py, crates/noesis-core/tests/contract_v1_authority.rs, ts-engines/src/server/registry.ts, package.json</read_first>
<action>The engines.json row set is the machine-readable authority; native/TS enumeration tests detect source drift against it. Reconcile discrepancies with the documented source/evidence rule before merging. Add negative fixtures for duplicate/missing IDs, wrong public grouping and wrong conditional/runtime classes. Compare the authoritative sets with actual native and TS registry enumeration. Exercise database-conditional registration in a harness owned by the existing Rust test file, using only a newly created disposable database or a local in-process registration fixture. Tests must require no production DATABASE_URL, secrets or CodeGraph service. Test setup and teardown may touch only owned temporary resources, must fail rather than skip required cases, and must verify registration both with and without DB configuration. Keep complete runtime capability/consumer catalogue conversion in Phase 3. Run the canonical gate and affected tests; record exact commands and scope.</action>
<verify><automated>pnpm run gate; affected native/TS enumeration tests pass; each mutated registry fixture is rejected with an actionable mismatch.</automated></verify>
<done>Source enumeration drift fails in CI without changing calculation semantics or any live service.</done>
</task>
</tasks>
<verification>Registry mutations fail, real enumeration matches, and six-axis rows remain evidence qualified. GATE-05 also requires plan 07.</verification>
<success_criteria>Registry authority is executable and portable; no engine or wave closes from this fixture alone.</success_criteria>
<output>Write 02-06-SUMMARY.md, update ISA and the phase verification, and retain any unavailable deployed evidence explicitly.</output>
