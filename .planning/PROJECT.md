# Selemene Engine — continuation

Continue the existing repository program, not a new product. Selemene owns Rust deterministic engines, TypeScript bridge engines, optional Python media services, API/orchestration, contracts, persistence/auth, SDK/CLI/TUI/admin distribution and declared Railway/Cloudflare infrastructure. Consumer repositories are compatibility context only.

Acceptance: root ISA.md, stable ISC IDs. Scope: docs/plans/selemene-engine/ROADMAP.md and CAPABILITY-LEDGER.md. GSD owns execution state in this directory. Fresh source and runtime probes may refute historical completion claims.

The user authorized the recommended research/discuss/plan/execute/verify flow, progressing until critical HITL. Use discuss --auto once per phase, retain recommended choices in CONTEXT.md, research with primary sources and verify plans before execution. Do not manufacture user approvals or researcher output.

Preserve source WIP/stash/worktrees; preserve all seven waves and 570 W3E IDs. No broad feature expansion before remaining registry/build gates. No secret values in artifacts or generation calls in health probes.

Critical boundaries: production-triggering merges/deployments, destructive/schema/data operations, expanded credential/security access, paid generation and material product-scope choices. Local reversible fixes and read-only provider inventory are authorized.
