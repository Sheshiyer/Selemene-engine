# Agent Task: Issue #247

> Auto-generated by agent-dispatch. Delete this file before the PR is merged.

## Issue
**Title:** [P4-W1-S3-07] Configure Alertmanager rules for engine latency p95 breaches
**URL:** https://github.com/Sheshiyer/Selemene-engine/issues/247

## Specification
<!-- plan-task-id: P4-W1-S3-07 -->
<!-- phase: P4 -->

## Configure Alertmanager rules for per-engine calculation latency p95 breaches

**Plan:** Performance & Observability  
**Phase:** `P4` | **Wave:** W1 | **Swarm:** Metrics & Alerting  
**Area:** `infra` | **Owner:** `Infra Eng`  
**Estimated Hours:** 2

### Context
A general API-level p95 latency alert already exists in `monitoring/prometheus/alerts/noesis-alerts.yml` (NoesisHighLatency, fires when `histogram_quantile(0.95, rate(noesis_request_duration_seconds_bucket[5m])) > 2`). Alertmanager routing config also exists.

**What is missing:** A per-engine alert that fires when a specific engine's `engine_calculation_duration` histogram p95 exceeds its SLO threshold.

### Deliverable
Add a new Prometheus alert rule `NoesisEngineLatencyP95Breach` using the `engine_calculation_duration_seconds` histogram grouped by `engine_id`, and verify Alertmanager routes it correctly.

### Acceptance Criteria
- New rule added to `monitoring/prometheus/alerts/noesis-alerts.yml`
- Rule uses `histogram_quantile(0.95, rate(engine_calculation_duration_seconds_bucket[5m])) by (engine_id)`
- Alert fires per engine_id independently
- Unit test in `monitoring/prometheus/tests/` validates the rule fires correctly
- Alertmanager `alertmanager.yml` routes the alert to the correct receiver

### Validation
`promtool test rules monitoring/prometheus/tests/` passes with the new alert test case

### Note
General p95 API latency alert (#244) is already implemented and closed. This issue is scoped to per-engine histogram granularity only.


## Agent Instructions
1. Read this file fully before writing any code.
2. Implement everything in the Deliverable section above.
3. Satisfy every item in the Acceptance Criteria section.
4. Run cargo build && cargo test and confirm all tests pass.
5. Delete .agent-tasks/issue-247.md when done.
6. Convert this PR from draft to ready-for-review.

## Done Signal
Post a PR comment: agent: done -- tests passing, ready for review