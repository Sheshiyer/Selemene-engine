# Agent Task: Issue #229

> Auto-generated by agent-dispatch. Delete this file before the PR is merged.

## Issue
**Title:** [P4-W1-S1-09] Create benchmark runner script with baseline capture
**URL:** https://github.com/Sheshiyer/Selemene-engine/issues/229

## Specification
<!-- plan-task-id: P4-W1-S1-09 -->
<!-- phase: P4 -->

## Create Criterion benchmark runner script with baseline capture

**Plan:** Performance & Observability  
**Phase:** `P4` | **Wave:** W1 | **Swarm:** Benchmarks  
**Area:** `infra` | **Owner:** `Backend Eng (Rust)`  
**Estimated Hours:** 3

### Context
`scripts/benchmark.sh` exists but is an HTTP API load tester (curl-based). It does NOT run `cargo bench` or capture Criterion baselines.

### Deliverable
A shell script `scripts/run-criterion-benchmarks.sh` that:
1. Runs `cargo bench --bench calculation_benchmarks -- --save-baseline main` (and any other bench targets)
2. Optionally compares against a saved baseline: `cargo bench -- --baseline main`
3. Outputs a summary table of regression/improvement per benchmark
4. Exits non-zero if any benchmark regresses beyond a configurable threshold (default 10%)

### Acceptance Criteria
- Script exists at `scripts/run-criterion-benchmarks.sh`
- Running it produces Criterion HTML reports in `target/criterion/`
- `--save-baseline` and `--compare-baseline` flags work
- Script documents how to capture an initial baseline and commit it

### Validation
Running `./scripts/run-criterion-benchmarks.sh --save-baseline initial` completes without error and produces output in `target/criterion/`

### Note
`scripts/benchmark.sh` (the HTTP curl-based API tester) is a separate tool and should not be modified.


## Agent Instructions
1. Read this file fully before writing any code.
2. Implement everything in the Deliverable section above.
3. Satisfy every item in the Acceptance Criteria section.
4. Run cargo build && cargo test and confirm all tests pass.
5. Delete .agent-tasks/issue-229.md when done.
6. Convert this PR from draft to ready-for-review.

## Done Signal
Post a PR comment: agent: done -- tests passing, ready for review