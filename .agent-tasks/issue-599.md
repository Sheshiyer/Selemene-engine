# Agent Task: Issue #599

> Auto-generated by agent-dispatch. Delete this file before the PR is merged.

## Issue
**Title:** docs: add 'Agent dispatch' section to README
**URL:** https://github.com/Sheshiyer/Selemene-engine/issues/599

## Specification
## Context

The repo has a fully automated agent dispatch pipeline (shipped in #568, #596, #597) but it's not documented in the main README. New contributors and users discovering the repo can't find the mechanism for the agent-ready workflow.

## Deliverable

Add a new section to `README.md` titled **Agent dispatch** placed after the existing CI badges (around line 27, depending on current state) and before the next major heading.

The section must explain:

1. **What it is** — an automated pipeline that turns labeled GitHub issues into draft PRs implemented by GitHub Copilot coding agent
2. **How to trigger it** — apply the `agent-ready` label to any open issue
3. **What happens** (in order):
   - `agent-dispatch.yml` creates branch `agent/issue-{N}-{slug}` off main
   - A draft PR opens with the issue spec + implementation checklist
   - A `@copilot` comment kicks off the GitHub Copilot coding agent (controlled by `COPILOT_AUTOKICK` repo var, default `true`)
   - Copilot implements the deliverable, deletes `.agent-tasks/issue-{N}.md` to signal done
   - `agent-auto-ready.yml` flips the PR from draft → ready-for-review
   - On merge, `agent-post-merge.yml` closes the linked issue + applies `agent-merged` label
4. **Backfill mode** — manual workflow dispatch processes pre-existing labeled issues; document the GitHub Actions UI path

## Acceptance Criteria

- [ ] New `## Agent dispatch` section in `README.md`
- [ ] Placement: after existing CI badges, before the next major heading
- [ ] Content covers all 4 points above
- [ ] Links to the four workflow files: `.github/workflows/agent-dispatch.yml`, `agent-auto-ready.yml`, `agent-merge-lane.yml`, `agent-post-merge.yml`
- [ ] Section length: under 60 lines (concise, scannable)
- [ ] No new emoji unless rest of README already uses them
- [ ] `cargo build` and `cargo test --lib` continue to pass (no Rust changes expected)

## Done Signal

After implementation, post a PR comment: `agent: done -- README section added, ready for review`

## Agent Instructions
1. Read this file fully before writing any code.
2. Implement everything in the Deliverable section above.
3. Satisfy every item in the Acceptance Criteria section.
4. Run cargo build && cargo test and confirm all tests pass.
5. Delete .agent-tasks/issue-599.md when done.
6. Convert this PR from draft to ready-for-review.

## Done Signal
Post a PR comment: agent: done -- tests passing, ready for review